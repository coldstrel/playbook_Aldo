// Ejemplo 2: for Each para calcular la suma de todos los elementos de la lista

let sum = 0;
const numbers = [1, 2, 3, 4, 5, 6, 7, 8, 25, 10];
numbers.forEach(num => sum += num);
console.log(`Suma de los Numeros: ${sum}`);